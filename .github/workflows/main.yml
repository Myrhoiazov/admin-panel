name: linting, testing, building, deploying
on:
    push:
        branches: [main]

jobs:
    pipeline:
        runs-on: ubuntu-latest
        strategy:
            matrix:
                node-version: [18.x] # Лучше использовать LTS

        steps:
            - uses: actions/checkout@v3

            - name: Setup Node.js
              uses: actions/setup-node@v3
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Install dependencies
              run: npm ci

            # - name: Build production project
            #   run: npm run build:prod

            # - name: Lint TypeScript
            #   run: npm run lint:ts

            # - name: Lint SCSS
            #   run: npm run lint:scss

            # - name: Unit testing
            #   run: npm run test:unit

            # - name: Build Storybook
            #   run: npm run storybook:build

            # - name: Screenshot testing
            #   run: npm run test:ui:ci

            # 7️⃣ Debug step: проверить SSH ключ

            # - name: Debug SSH key
            #   env:
            #         SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
            #   run: |
            #       echo "SSH key length: $(echo "$SSH_PRIVATE_KEY" | wc -c)"
            #       if [ -z "$SSH_PRIVATE_KEY" ]; then
            #           echo "❌ SSH_PRIVATE_KEY is empty! Check your GitHub Secret."
            #           exit 1
            #       else
            #           echo "✅ SSH_PRIVATE_KEY seems to be loaded correctly."
            #       fi
            # - name: Test SSH connection
            #   env:
            #         SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
            #   run: |
            #         mkdir -p ~/.ssh
            #         echo "$SSH_PRIVATE_KEY" | sed 's/\\n/\n/g' > ~/.ssh/id_ed25519
            #         chmod 600 ~/.ssh/id_ed25519
            #         ssh -i ~/.ssh/id_ed25519 -o StrictHostKeyChecking=no ${{ secrets.REMOTE_USER }}@${{ secrets.REMOTE_HOST }} "echo '✅ Connected!'"

            # - name: Deploy build folder to VPS
            #   if: github.ref == 'refs/heads/main'
            #   uses: easingthemes/ssh-deploy@v4
            #   with:
            #       SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
            #       REMOTE_HOST: ${{ secrets.REMOTE_HOST }}
            #       REMOTE_USER: ${{ secrets.REMOTE_USER }}
            #       TARGET: /var/www/project/frontend
            #       SOURCE: build/
